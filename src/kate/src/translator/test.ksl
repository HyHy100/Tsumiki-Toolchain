@group(0) @binding(0) 
buffer_ro buffer1: f32;

@group(0) @binding(1)
buffer_ro buffer2: fvec3;

@group(1) @binding(0)
buffer_wo buffer3: []{
    m0: vec4f,
    m1: vec4f,
    m2: vec3u,
    m3: vec3u
};

@group(1) @binding(1)
buffer_wo buffer4: []{
    m0: vec4f,
    m1: vec4f
};

// the '@compute' here means a compute shader entry point.
@compute @workgroup_size(1, 1, 1)
fn main(
    @builtin local_invocation_id: uvec3,
    @builtin local_invocation_index: u32,
    @builtin global_invocation_id: uvec3
) {
}